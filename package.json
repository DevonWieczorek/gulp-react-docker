{
  "name": "gulp-react",
  "version": "1.0.0",
  "description": "Test application for gulp and react in a docker env",
  "main": "./src/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^3.9.1"
  }
}
